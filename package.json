{
  "name": "training-videos",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "compounds": "^1.2.11",
    "cosmicjs": "^2.4.20",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14",
    "semantic-ui-react": "^0.74.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "export": "npm run build && cp extension.json build/extension.json && zip -r extension.zip build"
  }
}
